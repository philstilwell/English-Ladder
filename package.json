{
  "name": "english-ladder",
  "version": "1.0.0",
  "description": "A self-updating ESL website with daily lessons, grammar concepts, EFSP materials, and study tools.",
  "main": "app.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "build": "python3 editorial.py",
    "check:js": "node --check app.js && node --check learning.js && node --check tools.js && node --check us-life.js && node --check work.js && node --check work-ai.js && node --check work-ready.js && node --check site.js && node --check ai-practice.js && node --check study-routes.js",
    "test": "python3 -m unittest discover -s tests && node --test tests/*.test.cjs",
    "build:work": "python3 generate_work_documents.py && python3 generate_efsp_web_pages.py",
    "check:site": "python3 audit_site.py && python3 audit_seo.py",
    "build:seo": "python3 seo.py",
    "build:grammar": "python3 grammar_curriculum.py && python3 grammar_documents.py"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/philstilwell/English-Ladder.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/philstilwell/English-Ladder/issues"
  },
  "homepage": "https://github.com/philstilwell/English-Ladder#readme",
  "devDependencies": {
    "jsdom": "^26.1.0",
    "playwright": "^1.60.0"
  }
}
